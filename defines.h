#pragma once

#include <vector>
#include <string>

using byte = unsigned char;
using string = std::string;
template <typename T>
using vector = std::vector<T>;
using bytestring = vector<byte>;